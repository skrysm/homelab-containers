server:
  # Increase logging only for the container test to get more context if Unbound
  # exits during startup on a CI runner.
  verbosity: 3

  local-zone: "homelab.test." static
  local-data: "healthcheck.homelab.test. 60 IN A 1.2.3.4"
